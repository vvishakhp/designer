import { Injectable } from '@angular/core';
import { ActivityInfo } from '../models/activityInfo';

@Injectable()
export class WorkflowDesigner {
    public addActivity(activityInfo: ActivityInfo) {

    }
}
